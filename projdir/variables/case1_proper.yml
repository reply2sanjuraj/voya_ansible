---
- name: List used in an improper way
  hosts: web
  become: true
  tasks:
  - name: Install package
    ansible.builtin.yum: 
      name: 
        - httpd
        - firewalld
      state: latest
    register: pkg_status

  - name: Print the command
    ansible.builtin.debug:
      var: pkg_status
...

